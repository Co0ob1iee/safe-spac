{
  "name": "safe-spac-webapp",
  "private": true,
  "version": "0.0.1",
  "description": "Public front for safe-spac (static, served by nginx).",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "echo 'static only'"
  }
}
